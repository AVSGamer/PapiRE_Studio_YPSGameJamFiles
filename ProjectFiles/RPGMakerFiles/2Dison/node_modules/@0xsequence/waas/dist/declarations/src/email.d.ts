import { IdTokenIdentity } from "./auth.js";
export declare class EmailAuth {
    readonly region: string;
    readonly clientId: string;
    private cognitoMemo;
    constructor(region: string, clientId: string);
    private cognito;
    private signUp;
    private signIn;
    initiateAuth({ email }: {
        email: string;
    }): Promise<{
        email: string;
        instance: string;
    }>;
    finalizeAuth({ instance, email, answer, sessionHash }: {
        instance: string;
        email: string;
        answer: string;
        sessionHash: string;
    }): Promise<IdTokenIdentity>;
}
