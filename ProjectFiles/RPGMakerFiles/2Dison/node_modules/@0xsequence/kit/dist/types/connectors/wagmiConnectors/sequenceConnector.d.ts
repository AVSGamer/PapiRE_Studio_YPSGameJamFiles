import { sequence } from '0xsequence';
import { ETHAuthProof } from '@0xsequence/auth';
import { ChainIdLike } from '@0xsequence/network';
import { ConnectOptions } from '@0xsequence/provider';
import { EthAuthSettings } from '../../types';
export interface BaseSequenceConnectorOptions {
    walletAppURL?: string;
    defaultNetwork?: ChainIdLike;
    connect: ConnectOptions;
}
export declare function sequenceWallet(params: BaseSequenceConnectorOptions): import("wagmi").CreateConnectorFn<sequence.provider.SequenceProvider, {
    params: BaseSequenceConnectorOptions;
    setEmail: (email: string) => void;
}, {
    "@kit.ethAuthProof": ETHAuthProof;
    "@kit.theme": string;
    "@kit.ethAuthSettings": EthAuthSettings;
    "@kit.waasSignInEmail": string | null;
}>;
export declare namespace sequenceWallet {
    var type: "sequence";
}
//# sourceMappingURL=sequenceConnector.d.ts.map