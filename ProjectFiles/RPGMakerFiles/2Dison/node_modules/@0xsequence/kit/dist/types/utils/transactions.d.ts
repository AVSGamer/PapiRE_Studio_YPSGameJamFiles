import { PublicClient, WalletClient, Hex } from 'viem';
import { Connector } from 'wagmi';
interface Transaction {
    to: Hex;
    data?: Hex;
    value?: bigint;
}
interface SendTransactionsInput {
    chainId: number;
    senderAddress: Hex;
    publicClient: PublicClient;
    walletClient: WalletClient;
    connector: Connector;
    transactions: Transaction[];
    transactionConfirmations?: number;
    waitConfirmationForLastTransaction?: boolean;
}
export declare const sendTransactions: ({ chainId, senderAddress, publicClient, walletClient, connector, transactions, transactionConfirmations, waitConfirmationForLastTransaction }: SendTransactionsInput) => Promise<string>;
export {};
//# sourceMappingURL=transactions.d.ts.map