import React from 'react';
import { ModalPosition, Theme } from '../index';
type ThemeContext = {
    setTheme: React.Dispatch<React.SetStateAction<Theme>>;
    theme: Theme;
    position: ModalPosition;
    setPosition: React.Dispatch<React.SetStateAction<ModalPosition>>;
};
export declare const useThemeContext: () => ThemeContext, ThemeContextProvider: React.Provider<ThemeContext | undefined>;
export {};
//# sourceMappingURL=Theme.d.ts.map