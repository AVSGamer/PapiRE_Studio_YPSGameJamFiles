import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Box, NetworkImage, Text } from '@0xsequence/design-system';
import { getNetwork, getNetworkBackgroundColor, getNetworkColor } from '../../utils/networks';
export const NetworkBadge = ({ chainId }) => {
    const network = getNetwork(chainId);
    const chainColor = getNetworkColor(chainId);
    const chainBGColor = getNetworkBackgroundColor(chainId);
    return (_jsxs(Box, { height: "6", paddingX: "2", gap: "1", borderRadius: "xs", flexDirection: "row", justifyContent: "center", alignItems: "center", width: "fit", style: {
            background: chainBGColor
        }, children: [_jsx(NetworkImage, { chainId: chainId, size: "xs" }), _jsx(Text, { variant: "xsmall", fontWeight: "bold", style: {
                    color: chainColor
                }, capitalize: true, ellipsis: true, children: network.title ?? network.name })] }));
};
//# sourceMappingURL=NetworkBadge.js.map