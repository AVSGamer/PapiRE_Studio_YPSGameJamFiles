import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Box, Button, Text, useTheme } from '@0xsequence/design-system';
import { useConnect } from 'wagmi';
export const ExtendedWalletList = ({ onConnect, connectors }) => {
    const { theme } = useTheme();
    const { isPending } = useConnect();
    return (_jsx(Box, { flexDirection: "column", gap: "2", marginTop: "5", children: connectors.map(connector => {
            const Logo = theme === 'dark' ? connector._wallet.logoDark : connector._wallet.logoLight;
            const walletName = connector._wallet.name;
            const connectorId = connector._wallet.id;
            return (_jsx(Button, { width: "full", shape: "square", leftIcon: () => (_jsx(Box, { justifyContent: "center", alignItems: "center", style: { backgroundColor: connector._wallet.iconBackground }, width: "8", height: "8", overflow: "hidden", children: _jsx(Logo, { style: { width: 30 } }) })), onClick: () => onConnect(connector), label: _jsxs(Text, { children: [walletName, isPending] }) }, connectorId));
        }) }));
};
//# sourceMappingURL=ExtendedWalletList.js.map