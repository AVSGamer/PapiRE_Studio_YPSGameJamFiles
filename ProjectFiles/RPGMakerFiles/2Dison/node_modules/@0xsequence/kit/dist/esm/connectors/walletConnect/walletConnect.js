import { walletConnect as walletConnectbase } from 'wagmi/connectors';
import { WalletConnectLogo } from './WalletConnectLogo';
export const walletConnect = (options) => ({
    id: 'wallet-connect',
    logoDark: WalletConnectLogo,
    logoLight: WalletConnectLogo,
    // iconBackground: '#fff',
    name: 'Walletconnect',
    type: 'wallet',
    createConnector: () => {
        const connector = walletConnectbase({
            ...options
        });
        return connector;
    }
});
//# sourceMappingURL=walletConnect.js.map