import { sequenceWaasWallet } from '../wagmiConnectors/sequenceWaasConnector';
import { getEmailLogo } from './EmailLogo';
export const emailWaas = (options) => ({
    id: 'email-waas',
    logoDark: getEmailLogo({ isDarkMode: true }),
    logoLight: getEmailLogo({ isDarkMode: false }),
    name: 'Email',
    type: 'social',
    createConnector: () => {
        const connector = sequenceWaasWallet({
            ...options,
            loginType: 'email'
        });
        return connector;
    }
});
//# sourceMappingURL=emailWaas.js.map