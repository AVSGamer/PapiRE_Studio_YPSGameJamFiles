import { ComponentType } from 'react';
import { PolymorphicComponent } from '../Box';
import { IconProps } from '../../icons/types';
type TagProps = {
    label: string;
    icon?: ComponentType<IconProps>;
};
export declare const Tag: PolymorphicComponent<TagProps, 'div'>;
export {};
