import { RecipeVariants } from '@vanilla-extract/recipes';
export declare const buttonVariants: import('@vanilla-extract/recipes').RuntimeFn<{
    variant: {
        base: string;
        ghost: string;
        feature: (string | {
            outline: "2px solid rgba(255, 255, 255, 0.1)";
            outlineOffset: "-2px";
        })[];
        primary: string;
        glass: string;
        emphasis: string;
        raised: string;
        danger: string;
        text: (string | {
            outlineOffset: "1px";
        })[];
    };
    shape: {
        circle: string;
        square: string;
    };
    disabled: {
        true: string;
        false: string;
    };
    size: {
        xs: (string | {
            height: "28px";
        })[];
        sm: (string | {
            height: "36px";
        })[];
        md: (string | {
            height: "44px";
        })[];
        lg: (string | {
            height: "52px";
        })[];
    };
    iconOnly: {
        true: {
            padding: number;
            display: "flex";
            alignItems: "center";
            justifyContent: "center";
        };
    };
    hasLeftIcon: {
        true: {};
    };
    hasRightIcon: {
        true: {};
    };
    activeOutline: {
        light: {
            outlineStyle: "solid";
            outlineOffset: `calc(var(--${string}) * -1)` | `calc(var(--${string}, ${string}) * -1)` | `calc(var(--${string}, ${number}) * -1)`;
            outlineWidth: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            borderColor: "transparent";
            outlineColor: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
        };
        bold: {
            outlineStyle: "solid";
            outlineOffset: `calc(var(--${string}) * -1)` | `calc(var(--${string}, ${string}) * -1)` | `calc(var(--${string}, ${number}) * -1)`;
            outlineWidth: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            borderColor: "transparent";
            outlineColor: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
        };
    };
}>;
export type ButtonVariants = RecipeVariants<typeof buttonVariants>;
