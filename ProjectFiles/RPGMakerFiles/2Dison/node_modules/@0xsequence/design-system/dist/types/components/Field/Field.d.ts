import { ReactNode } from 'react';
import { PolymorphicComponent } from '../Box';
export interface FieldProps {
    id?: string;
    label?: string | ReactNode;
    description?: string | ReactNode;
    labelLocation?: 'left' | 'right' | 'top' | 'hidden';
    disabled?: boolean;
    required?: boolean;
    error?: string;
}
export declare const Field: PolymorphicComponent<FieldProps, 'div'>;
