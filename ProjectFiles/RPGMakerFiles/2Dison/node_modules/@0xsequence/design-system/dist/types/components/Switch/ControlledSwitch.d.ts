import { SwitchProps } from '@radix-ui/react-switch';
import { Control } from 'react-hook-form';
type ControlledSwitchProps = SwitchProps & {
    control: Control;
    name: string;
    onCheckedChange: (checked: boolean) => void;
    rules?: {};
};
export declare const ControlledSwitch: ({ control, defaultChecked, onCheckedChange, name, rules, ...switchProps }: ControlledSwitchProps) => import("react/jsx-runtime").JSX.Element;
export {};
