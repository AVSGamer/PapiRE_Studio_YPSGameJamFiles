import { Control } from 'react-hook-form';
import { PolymorphicProps } from '../Box';
import { TextInputProps } from './TextInput';
type ControlledTextInputProps = PolymorphicProps<TextInputProps, 'input'> & {
    control: Control;
    defaultValue?: string;
    name: string;
    rules?: {};
};
export declare const ControlledTextInput: ({ defaultValue, name, control, rules, ...inputProps }: ControlledTextInputProps) => import("react/jsx-runtime").JSX.Element;
export {};
