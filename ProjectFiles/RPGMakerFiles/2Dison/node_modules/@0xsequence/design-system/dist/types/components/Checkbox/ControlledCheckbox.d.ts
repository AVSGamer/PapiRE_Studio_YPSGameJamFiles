import { Control } from 'react-hook-form';
import { CheckboxProps } from './Checkbox';
type ControlledCheckboxProps = CheckboxProps & {
    control: Control;
    name: string;
    onCheckedChange: (checked: boolean) => void;
    rules?: {};
};
export declare const ControlledCheckbox: ({ control, defaultChecked, onCheckedChange, name, rules, ...checkboxProps }: ControlledCheckboxProps) => import("react/jsx-runtime").JSX.Element;
export {};
