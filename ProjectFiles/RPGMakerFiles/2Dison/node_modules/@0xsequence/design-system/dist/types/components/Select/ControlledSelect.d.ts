import { Control } from 'react-hook-form';
import { SelectProps } from './Select';
type ControlledSelectProps = SelectProps & {
    control: Control;
    defaultValue?: string;
    name: string;
    onValueChange: (value: string) => void;
    rules?: {};
};
export declare const ControlledSelect: ({ control, defaultValue, name, onValueChange, rules, ...selectProps }: ControlledSelectProps) => import("react/jsx-runtime").JSX.Element;
export {};
