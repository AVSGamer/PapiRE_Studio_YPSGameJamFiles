import { Control } from 'react-hook-form';
import { PolymorphicProps } from '../Box';
import { TextAreaProps } from './TextArea';
type ControlledTextAreaProps = PolymorphicProps<TextAreaProps, 'input'> & {
    control: Control;
    defaultValue?: string;
    name: string;
    rules?: {};
};
export declare const ControlledTextArea: ({ defaultValue, name, control, rules, ...rest }: ControlledTextAreaProps) => import("react/jsx-runtime").JSX.Element;
export {};
